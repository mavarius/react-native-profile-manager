import { StyleSheet } from 'react-native'
import { appPrimaryColor } from '../../styles/constants'

export default StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'space-between',
    alignItems: 'center',
    backgroundColor: appPrimaryColor
  }
})